<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- White background -->
    <item>
        <color android:color="@color/background_primary" />
    </item>

    <!-- Grass tile repeating at the top -->
    <item android:gravity="top">
        <bitmap
            android:src="@drawable/grass_tile_v2"
            android:tileMode="repeat"
            android:gravity="top" />
    </item>

    <!-- Shepherd and sheep image at the top right -->
    <item android:gravity="top|right">
        <bitmap
            android:src="@drawable/shepherd_and_sheep_v2"
            android:gravity="top|right" />
    </item>
</layer-list>
